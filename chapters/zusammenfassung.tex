%!TEX root = ../student_thesis.tex

\section*{Abstract}
% Kurzzusammenfassung / Abstract (max. eine Seite)
Electric machines operate at relatively low frequencies yet are typically driven by high-frequency power-electronic circuitry. The step size required to solve either problem is determined by their individual time constants, which are inversely proportional to their frequencies. Since the time constants of these problems may be orders of magnitudes apart, they are most efficiently time-integrated at different rates. Their differing character – one a complex machine, modelled as a distributed device with differential equations and simulated numerically using methods such as the Finite Element Method (FEM), the other a circuit solved by its own solver – motivates a partitioned co-simulation rather than a monolithic approach. This work couples the domains with a Waveform Relaxation (WR) scheme, in which an inductance-based reduced-order model of the field serves as the interface condition and, as a linearisation term, accelerates convergence. The scheme is implemented with Xyce, Sandia's open-source SPICE simulator, leveraging its checkpoint/restart feature to re-solve each time window across WR iterations. The implementation is verified against monolithic solutions for cases with varying circuit complexity and a dummy field solver. A parameter study over [window length/linearisation update / …] shows that [key result], from which [a concrete guideline, e.g. for choosing the window size] is derived.
% TODO: Deine Contribution ist die erste richtige "blackbox" Implementierung in einen Circuit simulator. Das sollte hier auch irgendwo stehen.


\section*{Acknowledgements}
I want to thank my supervisors, Prof. Dr. rer. nat. Sebastian Schöps (TU-Darmstadt) and Dr. Jörg Ostrowski (Siemens) for the support and guidance they offered me throughout this thesis.
Sebastian Schöps' dissertation and papers on (field-circuit) co-simulation lay the mathematical foundation for this work and have been a constant source of knowledge.
Jörg Ostrowski's idea of the \textit{accumulated time step} was integral to this thesis.
Thank you both.

\subsection*{Use of AI-based Tools}
In Chapter~\ref{ch:introduction} Claude (Anthropic) was used to improve the text quality and find suitable references.
In Chapters~\ref{ch:fundamentals}~and~\ref{ch:interface-discretisation} Claude was used to improve text structure and clarity, to reformulate passages I had drafted, and to point out inconsistent notation. In Chapter~\ref{ch:interface-discretisation} it was additionally used as a sounding board during the derivation as it flagged candidate inconsistencies and questioned steps, which I then verified, corrected, or rejected myself. All mathematical results, derivations, and their correctness are my own and were checked by me.
Grammarly was employed throughout to enhance writing quality, ensuring grammar, spelling, and style accuracy.
Claude was used to improve grammar and phrasing in Chapter 4.

A rudimentary version of the \texttt{C++} driver was originally written by myself and later extended massively using Claude Code, which I directed and reviewed to implement the many features the suite now offers, such as the different parameters, modes, and the user interface. I take full responsibility for the resulting code.